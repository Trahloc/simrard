// Integration test crate root. Add mod foo; for new test modules.
// Stub test so the crate is valid and runnable.
// To add integration tests that run the binary: use std::process::Command and depend on the binary by path.

#[cfg(test)]
mod tests {
    #[test]
    fn stub_workspace_integration_crate() {
        // Placeholder until real integration tests are added.
    }
}
